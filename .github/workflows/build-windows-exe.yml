name: Build Windows API exe

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  build-windows-exe:
    runs-on: windows-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build exe
        shell: pwsh
        run: scripts/build-windows-exe.ps1

      - name: Smoke test executable
        shell: pwsh
        run: |
          $stdout = "api-smoke.stdout.log"
          $stderr = "api-smoke.stderr.log"
          $process = Start-Process -FilePath "dist/patent-checker-api.exe" -ArgumentList "--no-open", "--port", "18080" -RedirectStandardOutput $stdout -RedirectStandardError $stderr -PassThru
          try {
            $response = $null
            for ($i = 0; $i -lt 30; $i++) {
              if ($process.HasExited) {
                break
              }
              try {
                $response = Invoke-RestMethod -Uri "http://127.0.0.1:18080/health"
                break
              }
              catch {
                Start-Sleep -Seconds 1
              }
            }
            if ($null -eq $response -or $response.status -ne "ok") {
              Write-Host "Process exited: $($process.HasExited)"
              if ($process.HasExited) {
                Write-Host "Exit code: $($process.ExitCode)"
              }
              if (Test-Path $stdout) {
                Write-Host "--- stdout ---"
                Get-Content $stdout
              }
              if (Test-Path $stderr) {
                Write-Host "--- stderr ---"
                Get-Content $stderr
              }
              throw "Health check failed"
            }
          }
          finally {
            if (!$process.HasExited) {
              Stop-Process -Id $process.Id -Force
            }
          }

      - name: Upload executable
        uses: actions/upload-artifact@v4
        with:
          name: patent-checker-api-windows
          path: dist/patent-checker-api.exe
          if-no-files-found: error

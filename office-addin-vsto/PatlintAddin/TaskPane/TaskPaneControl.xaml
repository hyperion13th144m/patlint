<UserControl x:Class="PatlintAddin.TaskPane.TaskPaneControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="340" FontFamily="Segoe UI" FontSize="13">
  <UserControl.Resources>
    <Style x:Key="PrimaryBtn" TargetType="Button">
      <Setter Property="Background" Value="#1f6f5b"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="BorderBrush" Value="#1f6f5b"/>
      <Setter Property="Padding" Value="10,5"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="1" CornerRadius="4"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="SecondaryBtn" TargetType="Button" BasedOn="{StaticResource PrimaryBtn}">
      <Setter Property="Background" Value="White"/>
      <Setter Property="Foreground" Value="#111827"/>
      <Setter Property="BorderBrush" Value="#d1d5db"/>
    </Style>
    <Style x:Key="SmallBtn" TargetType="Button" BasedOn="{StaticResource SecondaryBtn}">
      <Setter Property="FontSize" Value="11"/>
      <Setter Property="Padding" Value="5,2"/>
    </Style>
  </UserControl.Resources>

  <ScrollViewer VerticalScrollBarVisibility="Auto">
    <StackPanel Margin="14,14,14,24">

      <!-- Header -->
      <TextBlock Text="PatLint" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

      <!-- API URL -->
      <TextBlock Text="API URL" FontWeight="Bold" Margin="0,0,0,4"/>
      <Grid Margin="0,0,0,8">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="ApiUrlBox" Grid.Column="0" Padding="6,5"
                 BorderBrush="#d1d5db" BorderThickness="1"
                 TextChanged="ApiUrlBox_TextChanged"/>
        <Button Grid.Column="1" Content="保存" Style="{StaticResource SecondaryBtn}"
                Margin="6,0,0,0" Click="SaveApiUrl_Click"/>
      </Grid>

      <!-- Action buttons -->
      <WrapPanel Margin="0,0,0,8">
        <Button x:Name="CheckApiBtn" Content="API確認" Style="{StaticResource SecondaryBtn}"
                Margin="0,0,6,6" Click="CheckApi_Click"/>
        <Button x:Name="CheckDocBtn" Content="文書をチェック" Style="{StaticResource PrimaryBtn}"
                Margin="0,0,0,6" Click="CheckDocument_Click"/>
      </WrapPanel>

      <!-- Status -->
      <TextBlock x:Name="StatusText" Foreground="#6b7280" Margin="0,0,0,10" TextWrapping="Wrap"/>

      <!-- Summary pills -->
      <WrapPanel x:Name="SummaryPanel" Margin="0,0,0,12"/>

      <!-- Results area -->
      <StackPanel x:Name="ResultsPanel"/>

    </StackPanel>
  </ScrollViewer>
</UserControl>
